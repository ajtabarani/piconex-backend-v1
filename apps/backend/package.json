{
  "name": "@piconex/backend",
  "private": true,
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "start": "npm run build && node dist/main.js",
    "typecheck": "tsc --noEmit",
    "routes:iam": "tsx src/http/routes/iam/listIamRoutes.ts"
  },
  "dependencies": {
    "@piconex/iam": "*",
    "zod": "^3.24.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "express-oauth2-jwt-bearer": "^1.7.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6"
  }
}